# Python-side project configuration.
# See Cargo.toml for project metadata exported in the build process.
# See tasks.py for project tasks.

[build-system]
requires = ["maturin>=0.11,<0.12"]
build-backend = "maturin"

[tool.maturin]
manylinux = "2014"
