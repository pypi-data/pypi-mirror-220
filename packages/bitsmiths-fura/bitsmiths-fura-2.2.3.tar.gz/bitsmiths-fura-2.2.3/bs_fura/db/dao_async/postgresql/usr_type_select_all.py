# This file was generated by mettle.genes.db.GenPy3 [ver 2.2] on Thu Jul 20 09:12:34 2023
#  Target Database = postgresql
#
import datetime
import uuid
import time
import mettle.lib
import mettle.io
import mettle.db

from bs_fura.db.tables.usr_type import tUsrType

class dUsrTypeSelectAll:
    __slots__ = ("_dbcon", "_dbstmnt", "orec")

    def __init__(self, dbcon: mettle.db.IAConnect):
        """
        Constructor.

        :param dbcon: Mettle database connection object.
        """
        self._dbcon   = dbcon
        self._dbstmnt = None
        self.orec     = None

    async def __aenter__(self):
        """
        With statement enter.
        """
        return self

    async def __aexit__(self, type, value, traceback):
        """
        With statement exit.
        """
        await self._destroy()

    async def _destroy(self):
        if self._dbstmnt:
            await self._dbstmnt.destroy()
            self._dbstmnt = None

    async def exec(self) -> "dUsrTypeSelectAll":
        """
        Execute the query.

        :return: Self for convenience.
        """
        await self._destroy()

        self._dbstmnt = await self._dbcon.statement("UsrTypeSelectAll", self._dbcon.STMNT_TYPE_READ)

        self._dbstmnt.sql("""select
  id,
  descr,
  modified_by,
  tm_stamp
from
  fura.UsrType
order by
  id""")

        self._dbstmnt.bind_out("id", str, 32)
        self._dbstmnt.bind_out("descr", str, 128)
        self._dbstmnt.bind_out("modified_by", str, 128)
        self._dbstmnt.bind_out("tm_stamp", datetime.datetime)

        await self._dbcon.execute(self._dbstmnt)

        return self

    async def fetch(self, must_exist: bool = False) -> bool:
        """
        Fetches the next row from the executed query.

        :param must_exist:
        :return: True if a row was fetched, else False.
        """
        if not await self._dbcon.fetch(self._dbstmnt):
            if must_exist:
                raise mettle.lib.xMettle(f"Record not found: [table:UsrType, proc:SelectAll]", err_code=mettle.lib.xMettle.eCode.DBNotFound)
            return False

        self.orec = tUsrType()

        self.orec.id = self._dbstmnt.result[0]
        self.orec.descr = self._dbstmnt.result[1]
        self.orec.modified_by = self._dbstmnt.result[2]
        self.orec.tm_stamp = self._dbstmnt.result[3]

        return True

    async def fetch_iter(self) -> tUsrType:
        """
        Yield all the rows one at a time.

        :return: Iterator for output rows
        """
        while await self.fetch():
            yield self.orec

    async def fetch_all(self, out_list: tUsrType.List, clear_list: bool = True) -> int:
        """
        Fetch all the rows into the specified list and return the list count.

        :param out_list:
        :param clear_list:
        :return: Number of rows fetched.
        """
        if clear_list:
            out_list.clear()

        while await self.fetch():
            out_list.append(self.orec)

        return len(out_list)

