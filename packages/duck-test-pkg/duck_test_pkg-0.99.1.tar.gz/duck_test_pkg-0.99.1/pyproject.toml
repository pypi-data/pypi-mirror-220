[tool.poetry]
name = "duck-test-pkg"
version = "0.99.01"
description = "Test Package used in SW Eng. training. Do not install."
authors = []
readme = "README.md"
packages = [{include = "duck_test_pkg"}]

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
