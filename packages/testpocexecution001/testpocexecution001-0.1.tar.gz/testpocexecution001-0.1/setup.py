from setuptools import setup, find_packages

setup(
    name="testpocexecution001",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Add any dependencies required for your package here
    ],
)
