[tool.poetry]
name = "rpamaker-library"
version = "0.1.0"
description = ""
readme = "README.md"
packages = [{include = "rpamaker_library"}]
authors = ["RPAMAKER Dev Team <info@rpamaker.com>"]

[tool.poetry.dependencies]
python = "3.11.2"
python-dotenv = "^1.0.0"
python-decouple = "^3.8"
fastapi = "^0.96.0"
uvicorn = "^0.22.0"
requests = "^2.31.0"


[tool.poetry.group.dev.dependencies]
black = "^23.3.0"
isort = "^5.12.0"
pylint = "^2.17.4"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
