from setuptools import setup, find_packages

setup(
    name='Katy_email_utils',
    version='0.2.0',
    description='A library for email address validation and formatting.',
    author='Katayoun',
    packages=find_packages(),
    install_requires=[],
)