from setuptools import setup, find_packages

setup(
    name='customdbcred',
    version='0.5',
    packages=find_packages(),
    install_requires=[
        #list any dependencies

    ],
)
