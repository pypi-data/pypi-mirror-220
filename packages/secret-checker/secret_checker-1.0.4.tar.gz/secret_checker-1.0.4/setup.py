from setuptools import setup

setup(
    name='secret_checker',
    version='1.0.4',
    description='A Python module for checking if either of the secrets/environment variables include prod or dev or other env name.',
    author='Akash Deep',
    author_email='dakash98@outlook.com',
    packages=['secret_checker'],
    install_requires=[],
)
