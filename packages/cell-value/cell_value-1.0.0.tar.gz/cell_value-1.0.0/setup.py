from setuptools import setup, find_packages

setup(
    name='cell_value',
    version='1.0.0',
    description='inner cell value',
    author='cc',
    author_email='no@example.com',
    packages=find_packages(),
    install_requires=[

    ],
    python_requires='>=3.6',
)